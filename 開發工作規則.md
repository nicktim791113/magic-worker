# 開發工作規則（SOP）

> 這份文件定義我們每次開發的標準流程。**每次做修改，都照這個流程走一遍。**
> 目的：讓開發有紀錄、改壞了能還原、線上版本不會忘記更新。

---

## 🧭 一頁速查（每次照做）

1. **判斷大小** → 大修改先**備份**到 `備份/日期_說明/`
2. **改 + 測試**（`npm run dev` 開著，邊改邊看）
3. **更新 `更新紀錄.md`**（每次必做）；有影響使用方式就順手更新 `README.md`
4. **`git add -A` → `git commit -m "說明"` → `git push`**（每次必做）
5. **確認**：GitHub「Actions」分頁是綠勾、線上版有生效

---

## 一、先判斷：這次是「小修改」還是「大修改」

這決定「要不要先備份」。

| 類型 | 例子 | 要先備份嗎？ |
|------|------|:----------:|
| 🟢 **小修改** | 改一個數值（跳躍力、速度、重力）、改文字、調顏色、改一兩行 | **不用** |
| 🔴 **大修改** | 新增 / 刪除檔案、同時改好幾個檔案、會**連動到其他檔案**的功能、重構、改架構 | **要** |

> 🤔 不確定算大還小？只要符合「會動到不只一個檔案」或「怕改壞了回不去」其中之一，
> 就當成大修改，**先備份再說**。寧可多備份，不要少。

---

## 二、（只有大修改才做）開工前先備份

動手前，把這次**會改到的檔案**（或乾脆整個 `src/` 資料夾）複製一份，放到 `備份/` 資料夾裡：

```
備份/
  2026-06-01_重構戰鬥系統/
      （把要改的檔案複製進來）
```

- 命名規則：`備份/日期_這次要做什麼/`
- 萬一改壞了、想回到原狀，就從這個資料夾把舊檔案複製回去。

> 📌 `備份/` 只存在你自己電腦，**不會上傳到 GitHub**（已在 `.gitignore` 設定），純粹是保險。
> 補充：其實每一次 `git commit` 也是一個「還原點」，備份是給大修改的**額外雙保險**。

---

## 三、進行修改

- 一次專心做**一件事**，不要一次塞很多不相關的改動。
- 讓 `npm run dev` 一直開著，邊改邊在瀏覽器看效果。
- 改完先自己玩一玩、測一測，確定沒壞。

---

## 四、更新「文件與紀錄」 — 每次都要做 ✍️

1. **更新紀錄（必做）**：打開 `更新紀錄.md`，在**最上面**加一筆：
   - 日期、做了什麼、為什麼。
   - 如果是大修改，順手寫上「備份放在 `備份/xxx/`」。
2. **README（需要時）**：如果這次改動影響到「怎麼安裝 / 怎麼操作 / 專案結構 / 部署方式」，
   就一起更新 `README.md`，別讓說明過時。

---

## 五、完成後：提交並推送 — 每次都要做 🚀

自己測試 OK 後，在專案資料夾執行：

```bash
git add -A
git commit -m "清楚說明這次做了什麼"   # 例如：「加入第二關」、「修正跳躍手感」
git push
```

- commit 訊息要**看得懂**，未來回頭找才方便。
- `git push` 之後，**GitHub Actions 會自動建置並部署**，約 1～2 分鐘線上版就更新。

---

## 六、最後確認 ✅

1. 到 GitHub 儲存庫的 **Actions** 分頁，看最新一筆是不是**綠色勾勾**（成功）。
2. 打開線上網址，確認這次的改動真的有生效：
   <https://nicktim791113.github.io/magic-worker/>
   （如果沒變，可能是瀏覽器快取，按 `Ctrl + F5` 強制重新整理。）

---

## 七、常用指令小抄

```bash
npm run dev      # 本機開發（邊改邊看），網址通常是 http://localhost:5173
npm run build    # 在本機試打包（檢查有沒有錯）
git status       # 看目前改了哪些檔案
git add -A       # 把所有改動加入暫存
git commit -m "說明"   # 建立一個存檔點
git push         # 推上 GitHub（之後自動部署）
git log --oneline      # 看歷史紀錄（每個都是還原點）
```

> 這套規則也適用於其他專案（例如「魔法RPG / 星界旅人」），把這份檔案複製過去、改一下網址即可。
